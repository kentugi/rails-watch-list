<div class="container">

<h1>Your lists</h1>
<% @lists.each do |list| %>
  <ul>
    <li><%= link_to list.name, list_path(list.id)%></li>
  </ul>
<% end %>
<%= link_to 'Add new list', new_list_path %>
</div>
